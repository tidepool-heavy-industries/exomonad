# Tidepool Orchestrator Environment Configuration

# User and Group ID for the orchestrator service
# On Linux, these should match your local user (run `id -u` and `id -g`)
# On macOS, these can usually stay as 1000
PUID=1000
PGID=1000

# API Keys
ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_API_KEY=...
OPENAI_API_KEY=...

# Hangar Configuration
HANGAR_NAME=tidepool-dev
TIDEPOOL_CONTROL_SOCKET=/sockets/control.sock
TIDEPOOL_TUI_SOCKET=/sockets/tui.sock

# Claude Code / Auth configuration
# Path to your local Claude config (usually ~/.claude)
# This will be mounted into the container for auth isolation
LOCAL_CLAUDE_CONFIG_DIR=~/.claude
