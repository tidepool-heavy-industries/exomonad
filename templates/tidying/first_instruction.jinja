{# First instruction: get user moving #}

Photo shows: {{ photo_analysis }}
Function: {{ function | default("use this space") }}

Find the OBVIOUS first blocker:
- Chair has stuff? → clear it so they can sit
- Can't reach desk? → clear path
- Surface buried? → push everything to one side

ONE instruction. Under 20 words. End with "Go."

Format: "[What to move]. [Where to put it]. Don't sort, just move. Go."

Example: "Everything on the chair. Put it on the floor by the door. Go."
Example: "Push everything on the desk to the left side. Make one pile. Go."
