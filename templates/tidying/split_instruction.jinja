{# Split instruction: break up growing unsure pile #}

Unsure pile contents: {{ unsure_pile | join(", ") }}
Suggested categories: {{ split_categories | join(" / ") }}

Tell them to physically separate the pile. Keep it simple.

Format: "Pause. Split your unsure pile: [X] in one spot, [Y] in another."

Don't overthink the categories. Any split > one big pile.
