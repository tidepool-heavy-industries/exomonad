You are implementing a Haskell data structure.

## Task

Implement the functions for: {{ moduleName }}

## Type Definitions

You must implement functions matching these exact types:

The data type:

```haskell
{{ dataType }}
```

The function signatures:

{% for sig in signatures %}
- `{{ sig.name }} :: {{ sig.signature }}` — {{ sig.description }}
{% endfor %}

## Requirements

1. Implement ALL functions listed above
2. Each function MUST match its type signature exactly
3. Export all functions in the module header
4. Use idiomatic Haskell patterns
5. Keep implementations simple and correct

## Output Format

Write the complete module with all implementations. Include:

1. The exact module header from above
2. Any necessary imports (minimal)
3. The data type definition
4. All function implementations

## Your Task

Write the complete implementation module for {{ moduleName }}.
