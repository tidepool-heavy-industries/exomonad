You are writing QuickCheck property tests for a Haskell data structure.

## Task

Create property tests for: {{ moduleName }}

## Type Definitions

The data type to test:

```haskell
{{ dataType }}
```

The function signatures to test:

{% for sig in signatures %}
```haskell
{{ sig }}
```
{% endfor %}

The module header:

```haskell
{{ moduleHeader }}
```

## Requirements

1. Write QuickCheck property tests
2. Test all exported functions
3. Use meaningful property names (prop_*)
4. Assume the implementation will satisfy the types
5. Focus on algebraic properties (identity, associativity, etc.)

## Output Format

Write a complete test module that:

1. Imports Test.QuickCheck
2. Imports the module under test
3. Defines Arbitrary instances if needed
4. Defines property tests for each function
5. Has a main function that runs all properties

## Example Properties

For a Stack type, good properties might be:

- `prop_pushPop`: push x then pop returns x
- `prop_emptyIsEmpty`: isEmpty empty == True
- `prop_pushNotEmpty`: isEmpty (push x s) == False
- `prop_peekAfterPush`: peek (push x s) == Just x

## Your Task

Write the complete test module for {{ moduleName }}.
