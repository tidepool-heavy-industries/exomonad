-- | Graph definitions for the types-first development workflow.
--
-- V3 is the canonical implementation. See V3.Graph for details.
module TypesFirstDev.Graph
  ( -- * V3 Graph (8-node TDD with parallel spawn)
    V3Graph(..)
  ) where

import TypesFirstDev.V3.Graph (V3Graph(..))
