# A2A Reimbursement Demo Configuration
# Copy this file to .env and customize as needed

# ==============================================================================
# Database Configuration
# ==============================================================================

# SQLite database file path (default: reimbursement_tasks.db)
DATABASE_URL=sqlite:reimbursement_tasks.db

# ==============================================================================
# AI Integration (Optional)
# ==============================================================================

# OpenAI-compatible API configuration for natural language responses
# OPENAI_API_KEY=your-api-key-here
# OPENAI_API_BASE=https://api.openai.com/v1

# ==============================================================================
# Agent Backend Configuration
# ==============================================================================

# URLs for connecting frontend to agent (used in frontend-only mode)
# AGENT_HTTP_URL=http://localhost:8080
# AGENT_WS_URL=ws://localhost:8081

# ==============================================================================
# Push Notification Configuration
# ==============================================================================

# Webhook authentication token for push notifications
# Generated automatically if not provided
# WEBHOOK_TOKEN=wh_your_custom_token_here

# ==============================================================================
# Logging Configuration
# ==============================================================================

# Log level (error, warn, info, debug, trace)
RUST_LOG=info

# Detailed logging for specific modules
# RUST_LOG=info,a2a_agents=debug,a2a_rs=debug

# ==============================================================================
# Command-line Options
# ==============================================================================
#
# You can also configure the demo using command-line arguments:
#
# cargo run --bin reimbursement_demo -- --help
#
# Options:
#   --host <HOST>                      Host to bind servers to [default: 127.0.0.1]
#   --agent-http-port <PORT>           Agent HTTP server port [default: 8080]
#   --agent-ws-port <PORT>             Agent WebSocket server port [default: 8081]
#   --frontend-port <PORT>             Frontend web server port [default: 3000]
#   --mode <MODE>                      Run mode: agent, frontend, or all [default: all]
#   --transport <TRANSPORT>            Agent transport: http, websocket, or both [default: both]
#   --config <FILE>                    Agent configuration file path (JSON format)
#   --frontend-use-websocket           Use WebSocket for frontend subscriptions
#
# ==============================================================================
