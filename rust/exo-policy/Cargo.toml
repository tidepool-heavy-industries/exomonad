[package]
name = "exo-policy"
version = "0.1.0"
edition = "2021"
description = "Bucket-C decision logic — tools, roles, hooks, events — generic over the exo-caps traits."

[lib]
name = "exo_policy"
path = "src/lib.rs"

[dependencies]
exo-caps = { path = "../exo-caps" }

serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
thiserror = { workspace = true }
async-trait = { workspace = true }
chrono = { workspace = true }
tokio = { workspace = true }

# Pre-pinned for the Wave-3 tool layer: `Args` derives its MCP schema via JsonSchema.
schemars = { workspace = true }
