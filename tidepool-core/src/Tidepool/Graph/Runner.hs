{-# LANGUAGE StandaloneKindSignatures #-}

-- | Graph execution engine for list-based graphs.
--
-- This module is kept for backwards compatibility but is no longer used.
-- The record-based DSL doesn't use a separate Runner module - execution
-- is handled by the graph framework directly.
module Tidepool.Graph.Runner
  ( -- * Deprecated (use record-based DSL instead)
  ) where

-- This module is kept for backwards compatibility but exports nothing.
-- Graph execution for record-based graphs is handled differently.
