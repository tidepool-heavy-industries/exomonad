{# FunctionGemma scoring template - Turn 2 (User request) #}
{# Turn 1 (function declaration) is prepended by the interpreter #}

Rate this {{ edgeType }} for the query:
<escape>{{ query }}<escape>

Location: {{ location }}
Depth: {{ depth }}

Code:
<escape>
{{ snippet }}
<escape>

Hover info:
<escape>{{ hover }}<escape>

{% if typeInfo %}
Type context: {{ typeInfo }}
{% endif %}

{% if patterns %}
Pattern cases:
{% for p in patterns %}
  - {{ p }}
{% endfor %}
{% endif %}

{% if constraints %}
Instance constraints: {{ constraints | join(", ") }}
{% endif %}

Assess relevance (1-5), risk (1-5), and applicable tags.
